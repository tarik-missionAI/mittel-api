# Mitel API Mock Server - Environment Configuration
# Copy this file to .env and update with your values

# ============================================
# Authentication Settings
# ============================================

# Enable/disable authentication (true/false)
# Set to 'true' to require Bearer token for API access
REQUIRE_AUTH=true

# Secret key for JWT token generation
# IMPORTANT: Change this to a random secret in production!
SECRET_KEY=your-secret-key-change-this-in-production

# Token expiration time in seconds
# Default: 3600 (1 hour)
# Min: 60 (1 minute), Max: 604800 (7 days)
TOKEN_EXPIRATION=604800

# ============================================
# User Management Settings
# ============================================

# User management mode: simple, json, or env
# - simple: Uses hardcoded users in app.py
# - json: Loads users from a JSON file (recommended)
# - env: Loads users from environment variables
USER_MGMT_MODE=env

# Path to users JSON file (when USER_MGMT_MODE=json)
USERS_FILE=users.json

# ============================================
# Environment-based Users (when USER_MGMT_MODE=env)
# ============================================
# Format: username,password,account_id,role
# Example:
# MITEL_USER_1=admin@company.com,SecurePass123,ACCT001,admin
# MITEL_USER_2=user@company.com,UserPass456,ACCT002,user
# MITEL_USER_3=viewer@company.com,ViewPass789,ACCT003,viewer

# ============================================
# Server Settings
# ============================================

# Flask environment (development or production)
FLASK_ENV=production

# Flask debug mode (true/false) - DO NOT use in production!
FLASK_DEBUG=false

# Server port
PORT=5000

# Server host
HOST=0.0.0.0
